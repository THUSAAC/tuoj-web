#### 如何使用 "自定义测试"

"自定义测试" 是为了帮助选手了解评测机的环境. 每个选手每场比赛有 `64s` 进行自定义测试的机会. 每次选手可以上传一份代码, 一份标准输入和一份标准输出. 

选手可以自定义本次测试的时间限制 (至少 `1s`) 和内存限制 (至多 `1024MB`).

测试结束后, 选手将得到本次运行的时间消耗, 空间消耗, 程序输出与标准输出进行比对的结果. (如有不同, 会返回大约`10`字节的不同信息)

<hr/>

#### 如何提交多个答案 (针对提交答案题)

在选择上传答案时可以选中多个文件. 这些文件被加载到网页中后, 将 "答案编号" 与 "测试点信息" 中的答案编号一一对应即可.

特别的, 如果你选中的答案文件文件名为 %d.out (%d为一个数字), 那么网页将自动将该答案文件的 "答案编号" 识别为 %d.